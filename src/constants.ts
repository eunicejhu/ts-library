export const COLOR_REGEX = new RegExp(/(rgb)\((\d{1,3},\s*){2}\d{1,3}\)/);
export const RGB_REGEX = new RegExp(/^rgb/g);
export const SPACE_REGEX = new RegExp(/ /g);
export const ALPHA_REGEX = new RegExp(/\)$/g);
